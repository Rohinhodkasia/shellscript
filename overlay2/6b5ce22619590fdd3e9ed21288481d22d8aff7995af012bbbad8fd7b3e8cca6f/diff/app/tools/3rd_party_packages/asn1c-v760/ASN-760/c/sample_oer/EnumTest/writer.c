/**
 * This file was generated by the Objective Systems ASN1C Compiler
 * (http://www.obj-sys.com).  Version: 6.4.A, Date: 20-Nov-2010.
 */
#include "Test.h"
#include "rtxsrc/rtxCommon.h"
#include "rtxsrc/rtxPrint.h"

static int processResult (OSCTXT* pctxt, int stat, const char* filename)
{
   const OSOCTET* msgptr = pctxt->buffer.data;
   size_t len = pctxt->buffer.byteIndex;

   printf ("Hex dump of encoded record:\n");
   rtxHexDump (msgptr, len);

   if (stat < 0) {
      printf ("Encoding failed\n");
      rtxErrPrint (pctxt);
      return stat;
   }

   /* Write the encoded message out to the output file */
   stat = rtxFileWriteBinary (filename, msgptr, len);
   if (stat < 0) {
      printf ("Write to file failed\n");
      rtxErrPrint (pctxt);
   }

   return stat;
}

int main (int argc, char** argv)
{
   OSCTXT     ctxt;
   int        i, stat;
   OSBOOL     verbose = FALSE;
   OSOCTET    buffer[100];

   /* Process command line arguments */
   if (argc > 1) {
      for (i = 1; i < argc; i++) {
         if (!strcmp (argv[i], "-v")) verbose = TRUE;
         else if (!strcmp (argv[i], "-notrace")) {}
         else {
            printf ("usage: writer [-v]\n");
            printf ("   -v  verbose mode: print trace info\n");
            return 1;
         }
      }
   }

   /* Initialize context structure */
   stat = rtInitContext (&ctxt);
   if (stat != 0) {
      rtxErrPrint (&ctxt);
      return stat;
   }
   rtxSetDiag (&ctxt, verbose);

   /* Encode MyEnum sample */
   printf ("encode MyEnum value b(2)..\n");
   rtxInitContextBuffer (&ctxt, buffer, sizeof(buffer));
   stat = OEREnc_MyEnum (&ctxt, MyEnum_b);
   stat = processResult (&ctxt, stat, "MyEnum.dat");

   /* Encode MyExtEnum sample */
   if (stat >= 0) {
      printf ("encode MyExtEnum value 128..\n");
      rtxInitContextBuffer (&ctxt, buffer, sizeof(buffer));
      stat = OEREnc_MyExtEnum (&ctxt, 128L);
      stat = processResult (&ctxt, stat, "MyExtEnum.dat");
   }

   rtFreeContext (&ctxt);

   return stat;
}
