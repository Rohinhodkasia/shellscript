/**
 * This file was generated by the Objective Systems ASN1C Compiler
 * (http://www.obj-sys.com).  Version: 6.8.A, Date: 19-Dec-2014.
 */
#include "CryptographicMessageSyntax-2009.h"
#include "PKIXAttributeCertificate-2009.h"
#include "AttributeCertificateVersion1-2009.h"
#include "rtxsrc/rtxCommon.h"
#include "rtxsrc/rtxStreamFile.h"
#include "rtxsrc/rtxPrint.h" 
#include "rtxsrc/rtxPrintStream.h" 

void writeToStdout(void* pPrntStrmInfo, const char* fmtspec, va_list arglist) {
   vprintf(fmtspec, arglist);
   return;
}

int main (int argc, char** argv)
{
   /* PDU: CryptographicMessageSyntax-2009.ContentInfo */
   ContentInfo data;
   OSCTXT     ctxt, xmlCtxt;
   int        i, len, stat;
   OSBOOL     trace = TRUE;
   OSBOOL     verbose = FALSE;
   const char* xmlFilename = "message.xml";
   const char* filename = "message.dat";
   const OSOCTET* msgptr;

   /* Process command line arguments */
   if (argc > 1) {
      for (i = 1; i < argc; i++) {
         if (!strcmp (argv[i], "-v")) verbose = TRUE;
         else if (!strcmp (argv[i], "-o")) filename = argv[++i];
         else if (!strcmp (argv[i], "-notrace")) trace = FALSE;
         else {
            printf ("usage: writer [-v] [-o <filename>]  [-notrace]\n");
            printf ("   -v  verbose mode: print trace info\n");
            printf ("   -o <filename>  write encoded msg to <filename>\n");
            printf ("   -notrace  do not display trace info\n");
            return 1;
         }
      }
   }

   /* Initialize context structure for XML decoding */

   stat = rtXmlInitContext (&xmlCtxt);
   if (stat != 0) {
      rtxErrPrint (&xmlCtxt);
      return stat;
   }
   rtxSetDiag (&xmlCtxt, verbose);

   stat = rtxStreamFileOpen (&xmlCtxt, xmlFilename, OSRTSTRMF_INPUT);
   if (stat != 0) {
      rtxErrPrint (&xmlCtxt);
      return -1;
   }

   /* Call compiler generated decode function */

   asn1Init_ContentInfo (&data);

   stat = XmlDec_ContentInfo_PDU (&xmlCtxt, &data);
   if (stat != 0) {
      printf ("decode of data failed\n");
      rtxErrPrint (&xmlCtxt);
      rtxStreamClose (&xmlCtxt);
      return -1;
   }
   if (trace) {
      rtxSetPrintStream (&xmlCtxt, &writeToStdout, (void *) 0);
      asn1PrtToStrm_ContentInfo (&xmlCtxt, "ContentInfo", &data);
   }

   /* Initialize context structure for DER encoding */

   stat = rtInitContext (&ctxt);
   if (stat != 0) {
      rtxErrPrint (&ctxt);
      return stat;
   }
   rtxSetDiag (&ctxt, verbose);

   /* Encode - uses dynamic encode buffer*/
   xe_setp (&ctxt, 0, 0);

   len = asn1E_ContentInfo (&ctxt, &data, ASN1EXPL);
   if (len > 0) {
      msgptr = xe_getp (&ctxt);
      if (trace) {
         printf ("Hex dump of encoded record:\n");
         rtxHexDump (msgptr, len);
         
         printf ("Binary dump:\n");
         xu_dump2 (&ctxt, msgptr);
      }
   }
   else {
      rtxErrPrint (&ctxt);
      rtFreeContext (&ctxt);
      return (-1);
   }

   /* Write the encoded message out to the output file */
   stat = rtxFileWriteBinary (filename, msgptr, len);
   if (stat < 0) {
      printf ("Write to file failed\n");
      rtxErrPrint (&ctxt);
      rtFreeContext (&ctxt);
      return stat;
   }

   rtFreeContext (&ctxt);
   rtFreeContext (&xmlCtxt);

   return 0;
}
