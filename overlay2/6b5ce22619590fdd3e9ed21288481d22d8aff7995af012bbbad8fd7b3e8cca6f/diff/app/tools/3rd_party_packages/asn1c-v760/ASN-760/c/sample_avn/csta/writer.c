/**
 * This file was generated by the Objective Systems ASN1C Compiler
 * (http://www.obj-sys.com).  Version: 7.5.A, Date: 23-Jul-2020.
 */
#include "CSTA-ROSE-PDU-types.h"
#include "rtxsrc/rtxCommon.h"

#include "rtxsrc/rtxPrint.h"
#include "rtxsrc/rtxStreamFile.h"

int main (int argc, char** argv)
{
   CSTA_ROSE_PDU pdu;
   CSTA_ROSE_PDU_invoke invoke;
   MakeCallArgument makeCallArgument;

   OSCTXT     ctxt;
   int        i, stat;
   OSBOOL     trace = TRUE;
   OSBOOL     verbose = FALSE;
   const char* filename = "message.txt";

   /* Process command line arguments */
   if (argc > 1) {
      for (i = 1; i < argc; i++) {
         if (!strcmp (argv[i], "-v")) verbose = TRUE;
         else if (!strcmp (argv[i], "-o")) filename = argv[++i];
         else if (!strcmp (argv[i], "-notrace")) trace = FALSE;
         else {
            printf ("usage: writer [-v] [-o <filename>]  [-notrace]\n");
            printf ("   -v  verbose mode: print trace info\n");
            printf ("   -o <filename>  write encoded msg to <filename>\n");
            printf ("   -notrace  do not display trace info\n");
            return 1;
         }
      }
   }

   /* Initialize context structure */
   stat = rtInitContext (&ctxt);
   if (stat != 0) {
      rtxErrPrint (&ctxt);
      return stat;
   }
   rtxSetDiag (&ctxt, verbose);

   /* Populate data variables */
   asn1Init_MakeCallArgument (&makeCallArgument);
   makeCallArgument.callingDevice.t = T_DeviceID_dialingNumber;
   makeCallArgument.callingDevice.u.dialingNumber = "555-1212";
   makeCallArgument.calledDirectoryNumber.t = T_CalledDeviceID_notRequired;

   asn1Init_CSTA_ROSE_PDU_invoke (&invoke);
   invoke.m.argumentPresent = 1;
   invoke.invokeId.t = T_InvokeId_present;
   invoke.invokeId.u.present = 1;
   invoke.opcode.t = T_Code_local;
   invoke.opcode.u.local = 10;
   invoke.argument.t = T_CSTA_ROSE_PDU_types_OperationSet_makeCall;
   invoke.argument.u.makeCall = &makeCallArgument;

   asn1Init_CSTA_ROSE_PDU (&pdu);
   pdu.t = T_CSTA_ROSE_PDU_invoke;
   pdu.u.invoke = &invoke;

   if (trace) {
      printf ("The following record will be encoded:\n");
      asn1Print_CSTA_ROSE_PDU ("data", &pdu);
      printf ("\n");
   }

   /* Create file output stream */
   stat = rtxStreamFileCreateWriter (&ctxt, filename);
   if (stat < 0) {
      printf ("Create file output stream failed\n");
      rtxErrPrint (&ctxt);
      rtFreeContext (&ctxt);
      return stat;
   }

   /* Encode */
   stat = asn1AvnEnc_CSTA_ROSE_PDU (&ctxt, &pdu);
   if (stat < 0) {
      printf ("Encoding failed\n");
      rtxErrPrint (&ctxt);
      rtFreeContext (&ctxt);
      return stat;
   }
   else if (trace) {
      printf ("Output file written successfully.\n");
   }

   rtFreeContext (&ctxt);

   return 0;
}
