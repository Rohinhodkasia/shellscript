/**
 * This file was generated by the Objective Systems ASN1C Compiler
 * (http://www.obj-sys.com).  Version: 7.4.A, Date: 26-Jul-2019.
 */
#include "Extensions.h"
#include "rtxsrc/rtxCommon.h"

#include <stdio.h>
#include <stdlib.h>

#include "rtjsonsrc/rtJsonCppMsgBuf.h"

static int decodeFile(const char* filename, OSBOOL verbose)
{
   int stat;
   
   // Create ASN.1 message objects
   OSJSONDecodeBuffer decodeBuffer (filename);
   decodeBuffer.setDiag (verbose);
   ASN1T_PersonnelRecord data;
   ASN1C_PersonnelRecord PersonnelRecordPDU (decodeBuffer, data);

   stat = PersonnelRecordPDU.Decode ();
   if (0 != stat) {
      decodeBuffer.printErrorInfo ();
      return stat;
   }
   printf ("Decode of data was successful\n");
   printf ("Decoded record (%s):\n", filename);
   PersonnelRecordPDU.Print ("PersonnelRecord");
   
   return 0;
}

int main (int argc, char** argv)
{
   OSBOOL       trace = TRUE, verbose = FALSE;
   const char*  filename = "message.json";
   int          i, stat;

   // Process command line arguments
   if (argc > 1) {
      for (i = 1; i < argc; i++) {
         if (!strcmp (argv[i], "-v")) verbose = TRUE;
         else if (!strcmp (argv[i], "-i")) filename = argv[++i];
         else if (!strcmp (argv[i], "-notrace")) trace = FALSE;
         else {
            printf ("usage: reader [-v] [-i <filename>] [-notrace]\n");
            printf ("   -v  verbose mode: print trace info\n");
            printf ("   -i <filename>  read encoded msg from <filename>\n");
            printf ("   -notrace  do not display trace info\n");
            return 1;
         }
      }
   }

   stat = decodeFile(filename, verbose);
   if ( stat != 0 ) return stat;
   
   stat = decodeFile("message2.json", verbose);
   return stat;
}
